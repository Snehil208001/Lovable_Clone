# GCP deploy scripts (optional)

**Azure is the primary production target.** See [`deploy/azure/`](../azure/) and the root README **Production on Azure** section.

These GCP Cloud Run scripts are kept for anyone who still wants Google Cloud. They are not the default path.
